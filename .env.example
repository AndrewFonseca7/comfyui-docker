# Domain for Caddy (localhost for local dev, your-domain.com for production with auto-HTTPS)
DOMAIN=localhost

# Basic auth credentials for browser access
# Username
AUTH_USER=admin
# Password hash - generate with: docker run --rm caddy:2-alpine caddy hash-password --plaintext 'your-password'
AUTH_HASH=$2a$14$CHANGE_ME
